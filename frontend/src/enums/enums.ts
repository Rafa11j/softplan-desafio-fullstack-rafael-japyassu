export enum UserTypes {
  ADMINISTRATOR = 'ADMINISTRATOR',
  TRIATOR = 'TRIATOR',
  FINISHER = 'FINISHER',
}

export enum ProcessState {
  OPEN = 'OPEN',
  IN_PROGRESS = 'IN_PROGRESS',
  FINISHED = 'FINISHED',
}
